version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build:
    executor: python/default
    steps:
      - checkout
      - run:
          jupyter-nbconvert --execute ./Notebook.ipynb
      - store_artifacts:
          path: ./Notebook.html
